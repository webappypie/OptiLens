// Shared Android library configuration — applied by each core module's build.gradle.kts.
// This keeps module build files concise and avoids repetition.
// Usage: apply from: rootProject.file("gradle/android-library.gradle.kts")  — NOT used directly.
// Each module applies it inline via its own build.gradle.kts.
